/**
 * @deprecated Import from `@/lib/theme` instead.
 * Re-export kept so existing imports continue to work during the multi-theme migration.
 */
export {
  APP_THEMES,
  DEFAULT_THEME,
  isAppTheme,
  listThemes,
  resolveAppTheme,
  resolveTheme,
  type AppTheme,
  type ResolveThemeInput,
} from './theme';
